#Analizar el siguiente programa y explicar con tus palabras qué es lo que realiza. 

numeros = [8, 15, 3, 22, 7]
numeros.remove(max(numeros))
print(numeros)

#Lo que realiza el siguiente código es buscar el valor máximo dentro de la lista y como se utiliza la funcion .remove, lo quita, dejando la lista sin el número 22
